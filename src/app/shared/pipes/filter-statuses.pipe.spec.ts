import { FilterStatusesPipe } from './filter-statuses.pipe';

describe('FilterStatusesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStatusesPipe();
    expect(pipe).toBeTruthy();
  });
});
